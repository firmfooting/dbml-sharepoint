release: "1.0.0"
date: "2026-08-12"
deployer_version: "dbml-sharepoint/0.1.0"
schema_version: "1.0.1"
notes: |
  RACI matrix template. Bump schema_version on any DBML/mapping change,
  especially to the ConfirmationDue formula, which recalculates every
  existing row (see governance).
