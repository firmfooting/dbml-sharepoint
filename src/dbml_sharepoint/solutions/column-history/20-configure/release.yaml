release: "1.0.0"
date: "2026-09-05"
deployer_version: "dbml-sharepoint/0.1.0"
schema_version: "1.0.0"
notes: |
  Column history template. Bump schema_version on any DBML/mapping change
  before redeploying. A column removed or renamed here breaks every flow
  bound to its internal name, so treat a schema change as a flow change.
