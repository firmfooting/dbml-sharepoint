release: "1.0.0"
date: "2026-07-22"
deployer_version: "dbml-sharepoint/0.1.0"
schema_version: "1.0.0"
notes: |
  Risk register template. Bump schema_version on any DBML/mapping change —
  especially matrix changes, which re-rate existing rows (see governance).
