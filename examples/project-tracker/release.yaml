release: "1.0.0"
date: "2026-07-22"
deployer_version: "dbml-sharepoint/0.1.0"
schema_version: "1.0.0"
notes: |
  Project Tracker example release. The release tag and schema version are
  stamped into deploy.js, the manifest and the deployment summary so a site
  can always be traced back to the exact schema that provisioned it.
